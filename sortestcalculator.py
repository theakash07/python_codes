Expression=int(),str(),int()

#can use this too 
expression=input("Expression: ")
print("{:.1f}".format(eval(expression),1))
#eval function is used to evaluate the expression in a string
#formal function is used for formatting

